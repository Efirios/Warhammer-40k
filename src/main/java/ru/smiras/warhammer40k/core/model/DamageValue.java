/**
 * Интерфейс для представления урона оружия или способности в правилах Warhammer 40,000 10-й редакции.
 *
 * Реализует механику урона: может быть фиксированным (FixedDamage) или переменным через кубики
 * (DiceDamage: D3, D6+2, 2D6 и т.д.). Метод roll() возвращает конкретное значение урона при каждой атаке.
 *
 * Используется в WeaponProfile для характеристики Damage (D). Поддерживает все варианты урона из кодекса.
 */

package ru.smiras.warhammer40k.core.model;

public interface DamageValue {
    int roll();
}

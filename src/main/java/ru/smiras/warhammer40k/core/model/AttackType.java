/**
 * Перечисление типов атак в правилах Warhammer 40,000 10-й редакции.
 *
 * Реализует различия между атаками: Melee (ближний бой), Ranged (стрельба), Pistol (пистолетные),
 * Heavy, Assault и т.д. Влияет на правила стрельбы, ближнего боя, Advance & Shoot, Pistol in Engagement Range.
 *
 * Используется в WeaponProfile для указания типа оружия и в AttackContext для определения,
 * какие правила применяются к атаке (например, Pistol можно использовать в ближнем бою).
 */

/**
 * Перечисление типов атак в правилах Warhammer 40,000 10-й редакции.
 *
 * Реализует различия между атаками: NORMAL (стандартная), BLAST (взрывная), TORRENT (заливная).
 * Влияет на правила стрельбы, попадание по нескольким целям, расчёт урона и специальные правила.
 *
 * Используется в WeaponProfile для указания типа атаки и в AttackContext для определения,
 * какие правила применяются (например, BLAST может поражать несколько моделей).
 */

package ru.smiras.warhammer40k.core.model;

public enum AttackType {
    NORMAL,
    BLAST,
    TORRENT
}
